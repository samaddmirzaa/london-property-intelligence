# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
*.so
build/
dist/

# --- Jupyter ---
.ipynb_checkpoints/
*-checkpoint.ipynb

# --- Virtual environments (just in case) ---
.env
.venv/
env/
venv/

# --- Data: generated artefacts only ---
# Keep data/raw/ and data/external/ committed (or use Git LFS for the 33 borough files).
# Processed Parquet is reproducible from raw, so ignore it.
data/processed/
data/reference/*.parquet
*.parquet
*.feather

# --- Models: only registered/snapshot models go in models/ ---
# MLflow runs (mlruns/) are intentionally committed per the plan (§10.1)
# so the README can link to specific run IDs.
models/*.pkl
models/*.joblib

# --- IDE ---
.idea/
.vscode/
*.swp

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- Logs ---
*.log
logs/

# --- Secrets (you'll have API keys for Groq/Gemini in Phase D) ---
.env.local
*.env
secrets/
